import React, {Component} from 'react';

export default class AssetToolbar extends Component {
  constructor(props) {
    super(props);
  }

  // maps through the array of assets for the band, displaying each with a checkbox next to it. Checked boxes cause those assets to be saved to that event.

  render() {
    return (<div>MyComponent</div>);
  }
}
AssetToolbar.propTypes = {
};
