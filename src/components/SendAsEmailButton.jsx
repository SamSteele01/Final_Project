import React, {PropTypes} from 'react';

export default class SendAsEmailButton extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (<div>MyComponent</div>);
  }
}
SendAsEmailButton.propTypes = {
};
